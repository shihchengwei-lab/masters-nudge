{
  "type": "object",
  "properties": {
    "decision": {"type": "string", "enum": ["intervene", "pass"]},
    "current_choice": {"type": "string"},
    "structural_cost": {"type": "string"},
    "direction": {
      "type": "string",
      "description": "A responsibility boundary or existing seam, never a patch or step-by-step implementation."
    },
    "evidence": {
      "type": "array",
      "items": {"type": "string"}
    }
  },
  "required": ["decision", "current_choice", "structural_cost", "direction", "evidence"],
  "additionalProperties": false
}
