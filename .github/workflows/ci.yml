name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Compile entry-point scripts
        run: python -m py_compile buddy.py inject.py buddy_window.py
      - name: Run smoke tests
        run: python -m unittest test_buddy -v
