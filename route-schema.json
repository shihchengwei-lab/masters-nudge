{
  "type": "object",
  "properties": {
    "status": {
      "type": "string",
      "enum": ["finding", "no_finding"]
    },
    "lens": {
      "type": "string",
      "enum": ["simplicity", "reliability", "performance", "none"]
    }
  },
  "required": ["status", "lens"],
  "additionalProperties": false
}
