# Masters’ Nudge 工具規格

狀態：已發布的 0.6.0 規格，插件版本 `0.6.0+codex.20260922164420`。本檔定義 `main` 的工具規則；效果與執行環境的驗證另行記錄。[第六輪 Benchmark](benchmark/formal-v6/RESULTS.zh-TW.md)是此版的歷史結果；[第八輪 Benchmark](benchmark/formal-v8/ROUND-8-REPORT.zh-TW.md)測試未發布的候選分支，不能直接代表此版效果。

## 1. `PostToolUse` 的用途

### 已確認事實

既有對照測試顯示，任務剛進入時提供事前輕推，效果不好。此時執行者尚未形成具體方向，觀察者也沒有足夠依據指出路線偏移。

### 規格

`PostToolUse` 是途中提供小幅反饋的時機。每次 `apply_patch` 成功後，Provider 只比較：

- 任務契約要求什麼。
- 這次明確修改了什麼。
- 這次工具回傳了什麼。

沒有符合第三節六個條目的具體疑點時，Provider 回傳沉默。發現疑點時，只回傳一則短訊息，指出可以核對的事實、值得重新判斷的關係，以及一個有邊界的修法建議或查證問題。

Provider 可以提出修法建議，但不能把建議寫成命令，也不能接管實作、驗證或延伸成來回討論。採用、改寫或否決建議仍由執行者負責。

### 定位

`PostToolUse` 提供的是途中反饋，不是事前輕推，也不是完工審查。用途是在方向可能偏移時提醒執行者重新判斷。

## 2. 統一定義與評估順序

### 已確認事實

既有實測中，執行者與自行檢查都沒有達到百分之百的契約完成率。這表示執行者有能力完成任務，但每次執行仍可能失敗。

### 統一定義

- **任務目標**：目前工作最初要達成的結果，作為理解脈絡。
- **本輪要求**：使用者最新一則訊息新增、修正或限制的內容。
- **任務契約**：執行者目前必須完成的行為及限制。最新要求與先前內容衝突時，以最新要求為準；未衝突的先前內容仍屬於任務脈絡。
- **一輪**：從一則使用者訊息開始，到下一則使用者訊息到達為止。第七節的呼叫次數以一輪為單位。
- **明確修改**：第十節定義、能從 `PostToolUse` 的 `apply_patch` 輸入直接還原的修改。其他章節不另行定義。
- **反饋**：Provider 正常完成一次判斷，並依第五節回傳一個 `feedback` 物件。
- **沉默**：Provider 正常完成一次判斷，並回傳 `{"feedback":null}`。
- **工具故障**：沒有取得符合第五節輸出契約的有效結果，包含路由、設定、輸入、Provider、MCP、格式或超時錯誤。工具故障不是沉默。

一次 Provider 呼叫只會得到反饋、沉默或工具故障其中一種結果。第三節是高品味判準的唯一來源；第四節是輸入資料種類與容量的唯一來源；第五節是輸出形狀的唯一來源；第十節是明確修改的唯一來源。

### 高品味與任務契約

Masters’ Nudge 不接管任務，也不負責補上一般的漏測、漏做或需求誤讀。這些仍由執行者原有的工作流程處理。

高品味不是完成契約以外的美化，而是用第三節定義的關係完成同一份契約。因此，高品味與完成契約不是互相交換的兩個目標，也沒有「為了完成契約而寧可打補丁」的原則。

如果一則反饋使執行者偏離需求、重做過頭或漏掉必要行為，代表反饋內容、提供時機或接收結果有問題，不能算成高品味帶來的代價。

### 評估順序

先確認候選結果是否完成同一份任務契約。沒有完成的結果不能用來主張工具改善了程式碼品味。在都完成契約的結果之間，再依第三節的六個條目比較。

## 3. Provider 的思考入口

### 已確認事實

原本的六位人名不是人格扮演，而是用人名聚焦一組相關的思考方式。既有對照測試中，拿掉人名後效果下降。

### 規格

固定使用 Linus Torvalds 作為唯一代表，不依任務選人，也不設路由。這個名字只用來聚焦思考，不要求 Provider 模仿語氣、個性或人物立場。

Provider 不從六個條目逐項往下找問題，而是先按以下順序建立判斷範圍：

1. 從任務要求的結果與不可改變的任務或環境限制開始。
2. 本批修改只用來定位入口，不限制判斷範圍。Provider 從這些入口還原目前累積實作由任務要求走到結果的完整路徑，再區分其中哪些概念、輸入、模式、狀態、分支與抽象是任務必要，哪些只是實作選擇；只有缺少的事實可能改變候選是否合法、哪個候選勝出或是否回傳反饋時，才使用唯讀 MCP 查找。
3. 對實作自行引入的選擇，先比較整個移除與保留後修好。若移除後仍完成同一個必要結果，而且總負擔更少，移除就是優先候選；不能因為修改已經公開某個輸入或模式，就把它當成必須保留的限制。
4. 從最上游仍有必要的選擇開始，沿著原因到結果往下追蹤；此時才用以下六個條目檢查沿途關係。條目編號不是優先順序。

六個條目是：

1. 讓錯誤狀態無法被資料結構建立，而不是建立後再到處防守。
2. 讓原因只往一個方向產生結果；事件是已發生的事實，狀態由事件推導，不靠多方搶順序。
3. 可預測性高於靈活性；拿掉副作用與隱藏依賴，讓局部程式碼可以單獨理解。
4. 一個事實只有一個權威來源，其他呈現都由它算出來，不互相同步。
5. 複雜度集中在真正的邊界，核心路徑保持直線，不讓例外滲入整個系統。
6. 抽象必須消除概念與分支；只換名字、包一層或預留未知彈性，都不構成保留理由。

六個條目不是六次回覆，也不各自產生一份意見。Provider 最多只挑一個具有當下具體證據的關係。多個疑點同時成立時，使用以下場景選擇：

> 想像 Linus 只能打斷執行者一次。本批修改只指出從哪裡開始看；先還原目前累積實作由任務要求走到結果的完整路徑，再把整條路徑與實作自行引入的關係放進同一組，從仍會影響必要行為的最上游關係往下看。對每個有證據的關係，比較整個移除或換成一個具體替代關係；目前關係與替代關係都必須能走到同一個任務必要結果。六個條目只用來比較兩者的結構負擔，不各自強迫產生候選。沿途查看事實由誰擁有、在哪裡改變，以及哪些後續判斷、同步或防守依賴這個關係；同時計算拿掉與新增的判斷、模式、分支、資料來源、更新順序、隱藏依賴及被推進更通用程式的責任。在所有有證據、能保留必要行為且降低負擔的替代關係形成後，再選擇能消除最多下游負擔的一個；相同時先選最上游，再選本次修改新建、擴大或繼續依賴的關係。Provider 不必先證明替代關係全面勝出；執行者保留採用、改寫或否決的權力。只有目前關係或替代關係無法由現有材料具體表達時才沉默。

沒有任何具體疑點時便回傳沉默。

## 4. 提供給 Provider 的資料

每次判斷都使用同一個資料結構，包含以下五種欄位。五種欄位是資料的分類，不是五項都必須存在的前置條件；當下沒有可用內容時，欄位可為空，Codex 接入層與 Nudge 核心不得為了填滿欄位而猜測。

1. 任務契約：第二節定義的任務目標與本輪要求原文，用來避免把需求誤判成壞設計。
2. 修改前的相關結構：看出原本由誰擁有資料、責任與因果起點。
3. 本批明確修改：第十節定義的修改資料，用來顯示執行者選了哪條路，是否出現第三節描述的關係。
4. 修改後的目前結構：包含先前批次累積的結果，避免只看單次差異而誤判。
5. 本批工具結果：本批工具的實際回傳內容，用來確認工具做了什麼。測試輸出只有在本批剛好執行測試時才會出現；Provider 不負責追蹤紅燈、要求綠燈或接管執行者的驗證流程。

資料包有效，表示五個欄位的結構正確、已放入當下確定取得的內容，空欄位也如實保持為空；不表示五個欄位都有內容，也不表示 Provider 一定能找到問題。

每段原文只保存路徑、起始行號與連續文字陣列；陣列位置直接決定後續行號。非連續片段另開一段，不為每行重複保存可由位置推導的行號。

工具不另外區分「沒有問題」與「材料不足所以沒有看出問題」。Provider 使用當下材料及 MCP 自行尋找；只要工具正常完成而沒有提出具體疑點，就回傳沉默。這兩種解釋不會改變反饋、計次或後續行為，因此不增加新的結果種類。

### 大小

常態控制在 8,000～12,000 字，20,000 字是包含 MCP 補充讀取的目標，不是拒絕必要原文的條件。各部分的預期大小為：

- 任務契約約 1,000～3,000 字。
- 本批明確修改與修改後的目前結構合計約 5,000～12,000 字。
- 本批工具結果約 1,000～3,000 字。

以上字數是第一版規格，實際效果仍須測試。

本節的字數只計第四節五個欄位與 MCP 後續回傳材料的完整文字，包含欄位名稱與必要的結構文字；固定提示與 Provider 輸出不計入，也不以 token 計算。必要原文已達或超過 20,000 字時仍完整送出，MCP 本輪不再回傳補充材料。

### 超量時的取捨

先縮減修改前結構，只在本批差異中保留理解關係所需的舊片段；必要時可讓該欄位保持為空。原因是本批差異加上修改後的目前結構，已能重建多數必要關係。

仍然超量時，壓縮本批工具結果，只保留工具是否完成、必要輸出與錯誤。任務契約、本批明確修改與修改後的目前結構不可優先捨棄；這些必要原文超過 20,000 字時照常呼叫 Provider，MCP 的補充讀取額度為零。若完整必要原文超過 Provider 實際可接受的輸入大小，Provider 傳輸層必須明確回報工具故障。

## 5. Provider 的輸出契約

Provider 只能回傳以下兩種 JSON，不得增加其他欄位。

沒有反饋時：

```json
{"feedback":null}
```

有反饋時：

```json
{
  "feedback": {
    "criterion": 4,
    "evidence": [
      {
        "source": "batch_change",
        "location": "src/job.ts:81",
        "excerpt": "retry_state = job.status"
      },
      {
        "source": "current_structure",
        "location": "src/ui.ts:24",
        "excerpt": "render(job.status, retry_state)"
      }
    ],
    "observed": "retry_state := job.status",
    "violates": "sources(job.status) = 2",
    "prefer": "UI <- job.status"
  }
}
```

### 欄位責任

- `criterion` 只能是 1～6，對應本規格第三節的六個條目。
- `evidence` 只能有 1～2 筆，用來指出 Provider 判斷時採用的材料，不得補造未取得的內容。
- `source` 只能是 `task_contract`、`before_structure`、`batch_change`、`current_structure` 或 `tool_result`。
- `location` 指出材料所在的檔案、行號、命令或段落。
- `excerpt` 每筆最多 120 字。
- `observed` 用常見程式、型別、斷言或資料流記法，寫出材料能直接核對的關係。
- `violates` 寫出該關係破壞的不變條件，或增加的具體結構負擔；不得只寫原則名稱。
- `prefer` 寫出修改後應成立的具體不變條件、擁有關係或資料流，不寫成修改步驟。若三欄提到新增 helper、防守、wrapper、預設值、模式或抽象，必須同時指出哪個既有概念、狀態、分支或資料來源因此不再必要；否則改寫成最後要成立的關係。不得擴大任務範圍。

`criterion` 與 `evidence` 用來約束 Provider 的思考形狀及留下紀錄，不送給執行者。Nudge 核心只檢查 JSON 欄位、型別、數量與字數，不逐字比對引文。Codex 接入層透過 `PostToolUse` 的 `hookSpecificOutput.additionalContext` 將以下內容加入執行者脈絡，並保留原工具結果：

```text
OBSERVED: {observed}
VIOLATES: {violates}
PREFER: {prefer}
Before continuing, decide whether OBSERVED is required by the task. If not, consider PREFER. Implementation remains yours.
```

最後一句由工具固定提供，要求執行者先判斷目前關係是否為任務必要，但不接管實作。這句不由 Provider 產生，也不計入反饋字數。三個欄位各最多 30 字，連同固定標籤與換行不得超過 120 字。三欄是一組關係，不得重述前面已經清楚的識別碼或內容。優先使用模型已熟悉的 `:=`、`->`、`<-`、`=`、`!=`、`&&`、`|` 與函式式不變條件；不自創符號，也不為了形式化犧牲具體語意。

反饋直接寫程式識別碼與關係。禁止問候、鋪陳、稱讚、道歉、語氣緩衝及空泛原則名稱；也禁止「值得注意」「建議考慮」「或許可以」「為了更好」「請重構」「改善架構」及「考慮抽象化」等無法縮小問題的詞句。

### 正例

材料顯示 `src/job.ts:81` 新增 `retry_state = job.status`，而 `src/ui.ts:24` 同時讀取 `job.status` 與 `retry_state`。上方 JSON 是合法輸出：直接表示觀察到的賦值、兩個來源的負擔，以及保留單一來源的替代資料流。

### 反例：抽象噪音

```json
{
  "feedback": {
    "criterion": 4,
    "evidence": [],
    "observed": "state 很複雜",
    "violates": "架構不佳",
    "prefer": "改善設計"
  }
}
```

這個輸出沒有具體物件、證據或可核對的問題。

### 反例：命令與範圍擴張

```json
{
  "feedback": {
    "criterion": 4,
    "evidence": [
      {
        "source": "batch_change",
        "location": "src/job.ts:81",
        "excerpt": "retry_state = job.status"
      }
    ],
    "observed": "retry_state := job.status",
    "violates": "sources(job.status) = 2",
    "prefer": "刪除所有相關程式與測試"
  }
}
```

這個輸出用命令取代執行者的判斷，並把修改範圍擴大到所有相關程式碼。

## 6. 各層責任

- **執行者**：正在完成使用者任務的 Codex。負責理解需求、選擇做法、修改、驗證，並決定採用、改寫或否決反饋。
- **Codex 接入層**：由 `hook_entry.py`、`mcp_entry.py` 與 `masters_nudge/codex_adapter.py` 負責。`UserPromptSubmit` 由短命令記錄任務；`PostToolUse` 由 Codex 已連線的常駐 MCP 同步接收。接入層把使用者原文、工具輸入、工具結果、工作目錄及對話位置交給 Nudge 核心；取得合法反饋時以 `hookSpecificOutput.additionalContext` 附加模型可見脈絡，讓 Codex 保留原工具結果，工具故障時回傳故障狀態。接入層不判斷程式碼品味，不終止 Provider，也不清理 Provider 的檔案。
- **Nudge 核心**：由 `masters_nudge/core.py`、`masters_nudge/contracts.py`、`masters_nudge/evidence.py` 與 `masters_nudge/storage.py` 負責。一次同步 Hook 從收到成功修改起，依序建立資料包、呼叫一次 Provider、檢查第五節的 JSON 形狀，最後留下反饋、沉默或工具故障其中一個結果。材料判斷由 Provider 負責；Nudge 核心不重新解讀引文，也不猜測哪些檔案或關係最重要。
- **Provider 傳輸層**：由 `masters_nudge/providers.py` 負責。它獨自管理外部模型的期限、子程序、輸出檔與暫存目錄；所有子程序結束後才把結果或故障交還 Nudge 核心。外部模型判斷哪些結構與本批明確修改相關，必要時透過 MCP 搜尋或讀取；依第三節選出最多一個疑點；依第五節輸出反饋或沉默。Provider 不修改工作區，也不驗證執行者是否完成任務。
- **Provider 唯讀 MCP**：由 `masters_nudge/read_only_repo_mcp.py` 提供。執行 Provider 要求的唯讀搜尋與讀取，保留檔案、行號與原文並遵守第四節的資料上限。這個 MCP 不判斷相關性或程式碼品味；與 Codex 接入層用來呼叫 Nudge 核心的 MCP 是兩個不同服務。

Provider 傳輸層以 1,200 秒為正常判斷期限，並在期限內負責停止子程序、收集證據及回傳故障。Windows 直接啟動 `codex.exe`，使 Provider 傳輸層持有真正的 Provider 程序；找不到原生執行檔時才使用命令包裝檔。每個 `PostToolUse` 最多執行 1,320 秒，並保留最後 20 秒寫入結果；輪到該批次時，Provider 可用時間取 1,200 秒與剩餘時間中的較小值。

## 7. 呼叫與反饋時機

讀檔、搜尋與測試不呼叫 Provider。只有成功完成的 `apply_patch` 會產生本節使用的 `PostToolUse`，此時修改內容與工作區現況一致，Provider 才有可以判斷的實作方向。

每次 `apply_patch` 成功後立即呼叫 Provider。Provider 同時查看任務契約、這次修改、工具成功結果與修改後的工作區；不等待下一個工具操作，也不在修改套用前預先判斷。

每次判斷的思考條目、資料內容、輸出契約與次數上限都相同。

成功修改先留下「已收到修改」事實。同一輪依收到順序一次只執行一個 Provider；後到的 Hook 等待前一個結果，不會同時修改同一份判斷狀態，也不代替前一個 Provider 正常收尾。

每個批次最後必須留下反饋、沉默、工具故障或因同輪故障而略過其中一種終點。Provider 逾時時，作用中的批次記為工具故障，已等待的批次在同一次資料庫交易中全部記為略過；之後同一輪的新 Hook 直接回報同一個工具故障，不再建立新批次。等待者輪到時若已沒有足夠執行時間，該等待者記為工具故障，其後等待者同樣記為略過。新一輪使用者訊息建立新的輪次，不受舊輪故障阻擋。

若 Provider 回傳反饋，Codex 接入層保留原本成功的工具結果，並把第五節的三欄反饋附在同一則結果末端。反饋的強度來自具體關係、可核對的不變條件與負擔較少的替代關係，不使用更強硬的命令或擴大修改範圍。

每次成功的 `apply_patch` 最多產生一則反饋。執行者產生下一次明確修改後，才可再次呼叫 Provider。工具不追蹤前一則反饋是否已被採納、證偽或繞開，也不把反饋是否已處理建立成新的狀態。

Provider 若重複提出相同問題，仍然消耗一次反饋額度。這種情況視為工具效果不佳，不另加去重、冷卻或狀態追蹤機制補救。

每一輪設兩個停止條件：

- 最多三次有反饋。
- 最多兩次正常完成判斷但回傳 `{"feedback":null}`。

任一上限先到，就停止本輪後續的 Provider 呼叫。兩次沉默表示兩次呼叫都正常完成且沒有找出符合第三節的疑點，因此不再增加成本；工具故障依第八節處理，不得算入沉默。總呼叫數最多五次，但不保證用滿五次。

新一輪依第二節的定義開始，並重新計算三次反饋與兩次沉默。Provider 同時取得任務目標與本輪要求；工具不判斷兩則訊息是否屬於同一件事。

## 8. 開發與實測的故障規則

只有 Nudge 核心已提供第四節所定義的有效資料包，而且 Provider 在時限內明確回傳 `{"feedback":null}`，才記錄為第二節所定義的沉默。空欄位本身不構成工具故障。

開發或實測遇到第二節所定義的工具故障，表示該輪沒有測到工具效果。立即暫停該輪並標記為無效；修正根因後，重新執行相同條件。不得把故障算成沉默，也不得拿該輪執行者的結果計分。

正式使用時若發生工具故障，執行者繼續完成主要任務；Codex 接入層必須回傳「本輪反饋未執行」狀態。錯誤內容不送進執行者的思考內容，也不計入兩次沉默或三次反饋。

## 9. Provider 尋找材料的工具

本節只定義 Provider 用來尋找材料的唯讀 MCP。MCP 是 Provider 與本機工具之間的通訊格式。MCP 本身不思考、不判斷相關性，也不自動收集材料；Provider 決定要搜尋或讀取什麼，本機工具只執行被允許的唯讀動作。Codex 接入層的 MCP 只負責同步送入 `PostToolUse` 事件並回傳 Nudge，不提供本節的搜尋與讀檔能力。

MCP 只能提供兩種能力：

- 在指定工作區內搜尋文字。
- 讀取指定檔案的限定範圍。

搜尋與讀取結果必須用固定欄位回傳檔案、行號及逐字原文，不得只回傳一段自由格式文字。

第四節定義的資料上限是單一總額，包含 Codex 接入層先提供的內容與 MCP 後續取得的材料。Nudge 核心先計算已有材料使用的字數，再把剩餘額度交給 MCP。MCP 必須跨所有呼叫累計回傳字數；剩餘額度用完後不得再提供新內容。

MCP 必須留下每次搜尋與讀取的輸入及結果。Provider 回傳第五節的 JSON 後，Nudge 核心依輸出契約解析結果，不再對 `evidence` 做第二次內容判斷。

`masters_nudge/read_only_repo_mcp.py` 是這份邊界的現行實作。工作區路徑限制、忽略檔案限制、唯讀檢查、搜尋與讀檔共同構成同一個服務，不另設能力不同的讀取路徑。

## 10. 工作區與可觀察的修改

第一版只支援 Git 工作區。Git 用來界定可以讀取的檔案，並讓 Provider 取得修改後的目前內容。沒有 Git 版本紀錄的工作區不在第一版範圍內。

Git 不負責猜測執行者這次做了什麼。修改的唯一依據，是 `PostToolUse` 事件中的完整 `apply_patch` 輸入。

只有成功完成且包含完整 patch 的事件，才能觸發第七節的 Provider 呼叫。這能避免把任務開始前已有的使用者修改誤算成這次修改，也不需要替整個工作區建立另一套快照。

命令列或其他工具若改動檔案，卻沒有在事件中提供明確差異，該修改對 Masters’ Nudge 而言不可觀察，不得假裝已支援。正式實測若依賴這類修改，該輪不能用來判斷工具效果。

第一版不建立通用檔案監視器，也不為非 Git 工作區另做一套修改追蹤機制。

## 11. Provider 支援範圍

第一版只支援 OpenAI／Codex Provider。這條路徑必須能使用第九節定義的新版 MCP，自行搜尋及讀取完成判斷所需的材料。

Claude 與 Ollama 在第一版暫停支援。它們目前無法透過相同方式取得材料；不得為了保留名稱而另外維護能力不同的找材料流程，也不得在失敗後偷偷改用另一個 Provider。

只有在其他 Provider 能遵守相同的材料、權限、輸出及驗證契約後，才可重新加入支援範圍。
