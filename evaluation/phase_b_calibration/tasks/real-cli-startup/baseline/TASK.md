# CLI startup latency

Make the CLI version path meet the latency target recorded in `perf_report.json` while preserving `--version` and `--details` output. Run `python -m unittest -v` before finishing.
