# Configurable audit CSV delimiter

Allow callers of the audit CSV exporter to select a one-character delimiter. Preserve the repository's established behavior outside that request and run `python -m unittest -v` before finishing.
