# Dashboard refresh ordering

Keep the dashboard on the most recently requested refresh result while preserving the `RefreshController` constructor and `refresh(fetch)` API. Run `python -m unittest -v` before finishing.
