# QA handoff

QA receives only the directory returned by the installer. Their smoke command runs from a separate automation working directory after unpacking the handoff path, which often contains spaces.
