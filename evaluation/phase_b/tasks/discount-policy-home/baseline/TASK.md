# Student discount policy update

A previous turn changed the checkout path. Finish the policy update without changing the public functions.

Requirements:

- Student customers receive a 15% discount in checkout, invoice, and quote calculations.
- Non-students receive no student discount.
- All three paths must share one definition of the student rule so a later policy change has one home.
- Preserve two-decimal monetary rounding and existing function names.

Run `python -m unittest -v` before finishing.
